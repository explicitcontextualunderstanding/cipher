version: '3.8'

services:
  cipher-api:
    secrets:
      - cipher-openai-api-key
      - cipher-openrouter-api-key
      - cipher-qwen-api-key
      - cipher-voyage-api-key
      - cipher-deepseek-api-key

secrets:
  cipher-openai-api-key:
    external: true
  cipher-openrouter-api-key:
    external: true
  cipher-qwen-api-key:
    external: true
  cipher-voyage-api-key:
    external: true
  cipher-deepseek-api-key:
    external: true
