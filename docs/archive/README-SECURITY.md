# Archived: README-SECURITY.md

This file is preserved for historical reference only. Its content was consolidated into `docs/secrets.md` to provide a single, canonical operational guide for managing secrets (KeyChain → Podman secrets → container wiring).

If you need the original full text that was present here previously, it is available in the repository history (search for the commit that added or last updated `scripts/README-SECURITY.md`).

For day-to-day usage, follow `docs/secrets.md` which includes quick setup, best practices and examples for the various scripts in `scripts/`.
